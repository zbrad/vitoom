# Download service dependencies are intentionally small.
# Most shared communication dependencies live in vitoom-python-runtime.
# pillow/numpy: required by inference/common (logger, result_handler) at import time.
aiohttp>=3.9.0
aiofiles>=23.2.0
modelscope
huggingface_hub
pillow>=10.0.0
numpy>=1.24.0
